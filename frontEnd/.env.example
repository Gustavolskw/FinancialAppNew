# development: starts the Vite/React Router dev server.
# production: builds the React Router app and serves the compiled output.
FRONTEND_RUNTIME_MODE=development
FRONTEND_HOST=0.0.0.0
PORT=5173
VITE_API_BASE_URL=/api
CHOKIDAR_USEPOLLING=true
